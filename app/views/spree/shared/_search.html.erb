<% @taxons = @taxon && @taxon.parent ? @taxon.parent.children : Spree::Taxon.roots %>
<%= form_tag products_url, :method => :get, :class=>"form-search pull-right" do %>

  <%= search_field_tag :keywords, params[:keywords], :placeholder => t(:search), class: 'span8' %>
  <%= submit_tag t(:search), :name => nil, :class=>'btn btn-primary' %>
<% end %>
